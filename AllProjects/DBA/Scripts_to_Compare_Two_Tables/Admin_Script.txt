spool D:\GuruRaj\Gururaj_learnings\DBA\Scripts_to_Compare_Two_Tables\table_comparison_tab_admin.lst

Prompt Creating external directory and granting read and write permission on sgia_radar.
--Below statement should Run as sysdba

create or replace directory ext_dir as '/tmp'
/
grant read, write on directory ext_dir to sgia_radar
/

Prompt Creating table table_comparison_tab  as sgia_radar

create table table_comparison_tab (
history_table_names varchar2(2000),master_table_names varchar2(2000))
organization external (
  type              oracle_loader
  default directory ext_dir
  access parameters (
    records delimited  by newline
    fields  terminated by ','
    missing field values are null
  )
  location ('Master_History_table_Differences.csv')
)
reject limit unlimited
/

spool off;
